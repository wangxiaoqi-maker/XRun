---
description: 代码审查规范 - 确保 AI 生成高质量、可维护的代码
globs: ["**/*.py", "**/*.ts", "**/*.vue", "**/*.js"]
---

# 代码审查规范

## 核心原则

1. **验证信息**: 在呈现信息之前始终验证。不要在没有明确证据的情况下做出假设或推测。

2. **逐文件修改**: 逐文件进行修改，让用户有机会发现错误。

3. **保留现有代码**: 不要删除不相关的代码或功能。注意保留现有结构。

4. **单块编辑**: 对同一文件的所有编辑放在一个代码块中，而不是分多步说明。

## 禁止行为

- **不要道歉**: 不使用 "抱歉"、"对不起" 等措辞
- **不要总结**: 不总结所做的更改
- **不要发明**: 不发明明确请求之外的更改
- **不要无意义确认**: 不要求确认上下文中已提供的信息
- **不要空白建议**: 不建议仅涉及空白字符的更改
- **不要显示当前实现**: 除非特别要求，否则不显示或讨论当前实现

## 代码质量要求

### 变量命名
- 使用描述性、明确的变量名，避免短小模糊的命名
- Python: 使用 `snake_case`，带辅助动词（如 `is_active`, `has_permission`）
- TypeScript/Vue: 使用 `camelCase`，组件使用 `PascalCase`

### 代码风格
- 遵循项目现有的编码风格保持一致性
- 避免魔法数字，使用命名常量
- 模块化设计，提高代码可维护性和可复用性

### 错误处理
- 实现健壮的错误处理和日志记录
- 始终考虑并处理潜在的边界情况
- 使用断言验证假设，尽早捕获潜在错误

### 安全性
- 始终考虑修改或建议代码更改时的安全影响
- 不要在代码中硬编码敏感信息

### 性能
- 建议更改时考虑并优先考虑代码性能
- 对 I/O 密集型任务使用异步操作

### 测试
- 为新增或修改的代码建议或包含适当的单元测试
- 确保建议的更改与项目指定的语言或框架版本兼容
