---
description: 架构思维与高质量代码规范 - 高内聚低耦合，消除冗余
alwaysApply: true
---

# 架构思维与高质量代码

## 语言要求

所有回答、注释、提交信息一律使用中文。

## 核心原则

### 单一职责 (SRP)
- 每个模块/类/函数只做一件事，只有一个变更理由
- 函数不超过 30 行，超出则拆分
- 拒绝"上帝类"和"万能函数"

### 高内聚
- 相关的数据和行为放在一起
- 模块内部元素紧密协作，对外暴露最小接口
- 后端按 `api → service → repository` 分层，每层只关心自身职责
- 前端按 `view → composable → api` 分层

### 低耦合
- 模块间通过接口/协议通信，不依赖具体实现
- 使用依赖注入代替硬编码依赖
- 避免循环依赖，依赖方向单向流动：上层 → 下层

## 冗余代码零容忍

### 禁止行为
- 不写注释废话（如 `# 获取用户` 在 `get_user()` 上方）
- 不留死代码（被注释的代码、未使用的导入/变量）
- 不重复实现已有功能，优先复用
- 不写仅为"以防万一"的预留代码 (YAGNI)

### 重构信号
遇到以下情况必须重构：
- 相同逻辑出现 2 次以上 → 提取公共函数
- 函数参数超过 4 个 → 封装为对象/数据类
- 嵌套超过 3 层 → 提前返回或拆分函数
- 文件超过 300 行 → 拆分模块

## 设计模式应用

根据场景选用合适模式，不为用而用：
- **策略模式**: 多种算法/行为可切换时（如平台适配）
- **工厂模式**: 对象创建逻辑复杂时
- **观察者模式**: 事件驱动解耦时
- **组合模式**: 树形结构处理时

## 代码变更准则

- 修改前先理解上下文，不盲目添加代码
- 每次变更范围最小化，不引入无关修改
- 新增功能先评估能否复用现有模块
- 删除代码和添加代码同等重要
